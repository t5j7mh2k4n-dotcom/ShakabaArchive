@page
@model ShakabaArchive.Web.Pages.Account.InvitesModel
@{
    ViewData["Title"] = "أرقام الدعوة";
    var registerUrl = $"{WhatsAppShare.PublicSiteUrl}/Account/Register";
}

<div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
    <div>
        <h1 class="h4 mb-1">أرقام دعوة المستخدمين الجدد</h1>
        <p class="muted mb-0">الأدمن الرئيسي يضيف المستخدمين والثلاثة الموافقين عبر رقم دعوة، ثم يرسله عبر واتساب.</p>
    </div>
</div>

@if (!Model.IsAdmin)
{
    <div class="alert alert-warning">هذه الصفحة للأدمن الرئيسي فقط.</div>
}
else
{
    <div class="alert alert-info">
        الموافقون على صحة البيانات: <strong>@Model.ApproverCount</strong> / 3
        — باقي الدعوات لمدخلي البيانات (حفظهم بعد موافقة أحد الثلاثة).
    </div>

    <div class="card-panel mb-3">
        <form method="post" class="row g-2 align-items-end">
            <div class="col-md-5">
                <label class="form-label">ملاحظة (اختياري)</label>
                <input class="form-control" asp-for="Note" placeholder="مثال: دعوة لفلان" />
            </div>
            <div class="col-md-4">
                <label class="form-label">صلاحية المستخدم الجديد</label>
                <select class="form-select" asp-for="AssignRole">
                    <option value="@((int)UserRole.Editor)">مدخل بيانات (بعد موافقة أحد الثلاثة)</option>
                    <option value="@((int)UserRole.Approver)">موافق على صحة البيانات (أحد الثلاثة)</option>
                </select>
            </div>
            <div class="col-md-3">
                <button class="btn btn-accent w-100" type="submit">إنشاء رقم جديد</button>
            </div>
        </form>
        @if (!string.IsNullOrEmpty(Model.Error))
        {
            <div class="alert alert-danger mt-3 mb-0">@Model.Error</div>
        }
        @if (!string.IsNullOrEmpty(Model.LastCreatedCode))
        {
            <div class="alert alert-success mt-3 mb-0">
                <div class="mb-2">
                    الرقم الجديد: <strong style="font-size:1.3rem;letter-spacing:2px">@Model.LastCreatedCode</strong>
                    <span class="muted">(@RoleClaims.ToArabic(Model.AssignRole))</span>
                </div>
                <a class="btn btn-whatsapp"
                   href="@WhatsAppShare.InviteShareUrl(Model.LastCreatedCode, registerUrl)"
                   target="_blank" rel="noopener">
                    إرسال الرقم والرابط عبر واتساب
                </a>
            </div>
        }
    </div>

    <div class="card-panel">
        <h2 class="h6 mb-3">الأرقام</h2>
        <div class="table-responsive">
            <table class="table align-middle mb-0">
                <thead>
                <tr>
                    <th>الرقم</th>
                    <th>الصلاحية</th>
                    <th>ملاحظة</th>
                    <th>الحالة</th>
                    <th>تاريخ الإنشاء</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                @foreach (var c in Model.Codes)
                {
                    <tr>
                        <td><code>@c.Code</code></td>
                        <td>@RoleClaims.ToArabic(c.AssignRole)</td>
                        <td>@c.Note</td>
                        <td>@(c.IsUsed ? "مستخدم" : "متاح")</td>
                        <td>@c.CreatedAt.ToLocalTime().ToString("yyyy-MM-dd HH:mm")</td>
                        <td>
                            @if (!c.IsUsed)
                            {
                                <a class="btn btn-sm btn-whatsapp"
                                   href="@WhatsAppShare.InviteShareUrl(c.Code, registerUrl)"
                                   target="_blank" rel="noopener">واتساب</a>
                            }
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
}
